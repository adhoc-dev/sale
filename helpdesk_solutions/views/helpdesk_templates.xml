<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="helpdesk_followup_solution" inherit_id="helpdesk.tickets_followup" name="Helpdesk Solution" priority="100">
        <div t-field="ticket.description" position="attributes">
            <attribute name="t-field">ticket.ticket_description</attribute>
            <attribute name="t-if">not is_html_empty(ticket.ticket_description)</attribute>
        </div>
    </template>
</odoo>
