<?xml version="1.0"?>
<odoo>

    <record id="sale_subscription_template_view_form" model="ir.ui.view">
        <field name="name">sale.order.template.form</field>
        <field name="model">sale.order.template</field>
        <field name="inherit_id" ref="sale_subscription.sale_subscription_template_view_form"/>
        <field name="arch" type="xml">
            <field name="journal_id" position="before">
                <field name="dates_required" attrs="{'invisible': [('is_subscription', '=', False)]}"/>
            </field>
            <field name="journal_id" position="after">
                <field name="add_period_dates_to_description" attrs="{'invisible': [('is_subscription', '=', False)]}"/>
                <field name="recurring_auto_post" attrs="{'invisible': [('is_subscription', '=', False)]}"/>
            </field>
        </field>
    </record>

</odoo>
