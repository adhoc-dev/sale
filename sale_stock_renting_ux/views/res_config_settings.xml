<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.rental</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale_renting.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='rental_delay_costs']" position="before">
                <div class="col-12 col-lg-6 o_setting_box">
                    <div class="o_setting_left_pane">
                        <field name="rental_create_picking"/>
                    </div>
                        <div class="o_setting_right_pane">
                            <label for="rental_create_picking"/>
                            <span class="fa fa-lg fa-building-o" title="Values set here are company-specific." aria-label="Values set here are company-specific." groups="base.group_multi_company" role="img"/>
                            <div class="text-muted">
                            On a sale confirmation, by default, odoo creates only the required stock moves. With this option enabled, it will also create an Stock Transfer for those stock moves. This is useful when you need to print a legal voucher to delivery goods
                            </div>
                            <div class="content-group" attrs="{'invisible': [('rental_create_picking','=',False)]}">
                                <div class="mt8">
                                    <field name="rental_picking_type_id" placeholder="Default Picking for rentals"/>
                                </div>
                            </div>
                        </div>
                </div>
            </xpath>
        </field>
    </record>

</odoo>
